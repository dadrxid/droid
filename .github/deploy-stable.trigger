# Touch this file and push to master to publish ghcr.io/dadrxid/droid:stable (CI must pass first).
# Or use GitHub → Actions → Deploy Stable → Run workflow.
deploy: 2026-06-28T00:05:00Z commit=minimal-welcome-card
